// attempt to repeat answer to method minWindow
var minWindow = function(s, t) {
  let min = '';
  let temp = '';
  let left = 0;
  let right = 0;
  const qS = [];
  const hashT = {};

  if(s.length < t.length) return false;
  if(s[left])

  // Fill up hash for t
  for(let i of t){
      if(hashT[i]) hashT[i]++;
      else hashT[i] = 1;
  }

  // traverse s
  while(left < s.length){
      let sval = s[right];
      if(hash[sval] > 0){
          hash[sval]--;
          right++;
          temp
      } else{
          left++;
      }
      if(done){
          if(Max.min(min, ))
      }
  }
};